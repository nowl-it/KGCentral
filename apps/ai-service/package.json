{
	"name": "@kgcentral/ai-service",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 5000",
		"build": "python -m compileall src/",
		"start": "uvicorn src.main:app --host 0.0.0.0 --port 5000",
		"lint": "ruff check src/",
		"typecheck": "pyright src/"
	}
}
